@AGENTS.md

This repo drives a Bubble.io app through its private editor API. Read AGENTS.md first.
If the `bubble` MCP server is connected, prefer its `bubble_*` tools; otherwise use `node bin/bubble.ts`.
